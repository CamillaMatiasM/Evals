{
    "graders_types": [
        {
            "type": "string_check",
            "name": "Match output to expected answer",
            "input": "{{ sample.output_text }}",
            "operation": "eq",
            "reference": "{{ item.expected }}"
        },
    ]
}